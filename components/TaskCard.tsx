interface TaskCardProps {
  title?: any
  status?: any
  dueDate?: any
  clickable?: any
}

export default function TaskCard({ title, status, dueDate, clickable }: TaskCardProps) {
  return (
    <div className="">
      {/* TaskCard component content */}
    </div>
  )
}